name: Go Test on Pull Requests
on:
  pull_request:
    types:
      - opened
      - synchronize
      - reopened
jobs:
  go:
    uses: johnbieren/application-service/.github/workflows/pr.yml@make_github_action_reusable
